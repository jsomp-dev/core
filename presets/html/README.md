# JSOMP Standard Presets (HTML)

This directory contains the **free, open-source** presets for the JSOMP engine.

## HtmlRegistry
Provides basic mapping for standard HTML elements (`div`, `span`, `p`, etc.).
It allows the JSOMP engine to render content without any external UI library dependencies.

## Usage
This preset is included by default in the core distribution.
